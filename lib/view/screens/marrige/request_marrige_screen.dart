import 'package:flutter/material.dart';
class requestMarrigeScreen extends StatelessWidget {
  const requestMarrigeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Container(
        child: Text('requestMarrigeScreen')
      ),
    );
  }
}
